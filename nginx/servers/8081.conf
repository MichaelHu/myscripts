server {

    listen 8080;
    server_name localhost;

    location /apollo {
        # expires max;
        expires epoch;
        # expires @20h50m;
        # expires -1s;
        alias /Users/hudamin/projects/git/hugeapp-apollo/dist;
    }

    location /abc {
        expires epoch;
        rewrite ^\/abc(.*) /apollo$1;
    }

}
